<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h1>Your Pragmatic Catalog</h1>
<% @products.each do |f| %>
  <div class="entry">
    <%= image_tag(f.image_url) %>
    <h3><%= f.title %></h3>
    <%= sanitize(f.description) %>
    <div class="price_line">
      <span class="price"><%= number_to_currency(f.price) %></span>
    </div>
  </div>
<% end %>